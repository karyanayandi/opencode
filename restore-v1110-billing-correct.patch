diff --git a/packages/opencode/src/plugin/copilot.ts b/packages/opencode/src/plugin/copilot.ts
index 39ea0d00d..32f1573cf 100644
--- a/packages/opencode/src/plugin/copilot.ts
+++ b/packages/opencode/src/plugin/copilot.ts
@@ -69,6 +69,16 @@ export async function CopilotAuthPlugin(input: PluginInput): Promise<Hooks> {
               try {
                 const body = typeof init?.body === "string" ? JSON.parse(init.body) : init?.body
 
+                // A user message whose content consists entirely of tool results is a
+                // synthetic wrapper created by opencode (required by Anthropic/OpenAI APIs
+                // to pair every tool_use with a tool_result). These should be treated as
+                // agent-initiated, not user-initiated, to avoid charging premium requests.
+                const isSyntheticToolResult = (last: any) =>
+                  last?.role === "user" &&
+                  Array.isArray(last.content) &&
+                  last.content.length > 0 &&
+                  last.content.every((part: any) => part?.type === "tool_result" || part?.type === "tool")
+
                 // Completions API
                 if (body?.messages && url.includes("completions")) {
                   const last = body.messages[body.messages.length - 1]
@@ -77,7 +87,7 @@ export async function CopilotAuthPlugin(input: PluginInput): Promise<Hooks> {
                       (msg: any) =>
                         Array.isArray(msg.content) && msg.content.some((part: any) => part.type === "image_url"),
                     ),
-                    isAgent: last?.role !== "user",
+                    isAgent: last?.role !== "user" || isSyntheticToolResult(last),
                   }
                 }
 
@@ -89,15 +99,13 @@ export async function CopilotAuthPlugin(input: PluginInput): Promise<Hooks> {
                       (item: any) =>
                         Array.isArray(item?.content) && item.content.some((part: any) => part.type === "input_image"),
                     ),
-                    isAgent: last?.role !== "user",
+                    isAgent: last?.role !== "user" || isSyntheticToolResult(last),
                   }
                 }
 
                 // Messages API
                 if (body?.messages) {
                   const last = body.messages[body.messages.length - 1]
-                  const hasNonToolCalls =
-                    Array.isArray(last?.content) && last.content.some((part: any) => part?.type !== "tool_result")
                   return {
                     isVision: body.messages.some(
                       (item: any) =>
@@ -111,7 +119,7 @@ export async function CopilotAuthPlugin(input: PluginInput): Promise<Hooks> {
                               part.content.some((nested: any) => nested?.type === "image")),
                         ),
                     ),
-                    isAgent: !(last?.role === "user" && hasNonToolCalls),
+                    isAgent: last?.role !== "user" || isSyntheticToolResult(last),
                   }
                 }
               } catch {}
